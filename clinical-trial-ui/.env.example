# Copy this file to .env.local for local development.
# Leave VITE_API_URL unset locally — the Vite dev-server proxy handles /api/* automatically.
# Set it only when building for production (Vercel will pick it up automatically).

# VITE_API_URL=https://your-api.onrender.com
